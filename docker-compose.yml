version: "3.8"

services:
  nexus-forecast:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexus-forecast-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ./outputs:/app/outputs
